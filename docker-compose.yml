services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - svs-data:/data
    environment:
      NODE_ENV: development
      DATABASE_PATH: /data/soulvssoul.db
      SESSION_SECRET: dev-secret-change-me-in-production-please-32chars
      ALLOWED_ORIGINS: http://localhost:3000

volumes:
  svs-data:
